// Import everything from openid-client to see what's available
import * as openidClient from 'openid-client';

// Log the structure of the imported module
console.log('openidClient structure:', Object.keys(openidClient));